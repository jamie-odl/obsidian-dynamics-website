name: CI

on:
  push:
  pull_request:

jobs:
  smoke:
    runs-on: [self-hosted, linux, obsidian]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm test
